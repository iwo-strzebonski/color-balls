/**
 * Interface used to apply special ball functions.
 * Or something...
 * Because otherwise it's useless.
 */
export default interface IBallSpecials {
    grayscale: boolean
    animatable: boolean
}
